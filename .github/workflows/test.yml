name: "Dummy"

on:
  workflow_dispatch:
    inputs:
      sleep_time:
        description: "sleep for"
        default: 60
      runner_label:
        description: "runner label"
        default: ubuntu

jobs:
  dummy:
    name: "Do nothing, really"

    runs-on:
      - "${{ github.event.inputs.runner_label }}"

    steps:
      - name: "Just print some info"
        shell: bash
        run: |
          hostname
          uname -a
          df -h
          whoami
          sleep ${{ github.event.inputs.sleep_time }}
  dummy2:
    name: "Do nothing, really2"

    runs-on:
      - "${{ github.event.inputs.runner_label }}"

    steps:
      - name: "Just print some info"
        shell: bash
        run: |
          hostname
          uname -a
          df -h
          whoami
          sleep ${{ github.event.inputs.sleep_time }}
  dummy3:
    name: "Do nothing, really3"

    runs-on:
      - "${{ github.event.inputs.runner_label }}"

    steps:
      - name: "Just print some info"
        shell: bash
        run: |
          hostname
          uname -a
          df -h
          whoami
          sleep ${{ github.event.inputs.sleep_time }}
  dummy4:
    name: "Do nothing, really4"

    runs-on:
      - "${{ github.event.inputs.runner_label }}"

    steps:
      - name: "Just print some info"
        shell: bash
        run: |
          hostname
          uname -a
          df -h
          whoami
          sleep ${{ github.event.inputs.sleep_time }}
  dummy5:
    name: "Do nothing, really5"

    runs-on:
      - "${{ github.event.inputs.runner_label }}"

    steps:
      - name: "Just print some info"
        shell: bash
        run: |
          hostname
          uname -a
          df -h
          whoami
          sleep ${{ github.event.inputs.sleep_time }}
